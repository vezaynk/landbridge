{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "//Meta": "docket-meta's OWN Postgres (spec §3). Separate from any instance's database. Override via DOCKET_META_DB. Defaults to Host=localhost;Database=docket_meta;Username=docket.",
    "Meta": ""
  },
  "Meta": {
    "//Operator": "Operator auth for the panel (design note §5). Human-only, separate credential class from the plane.",
    "Operator": {
      "//PassphraseHash": "SHA-256 hex of the meta operator passphrase. Store the HASH, never the plaintext. When unset the login door is fail-closed (503). Generate e.g.: printf '%s' 'your-passphrase' | shasum -a 256.",
      "PassphraseHash": ""
    },
    "//Domain": "Base domain instances publish under: instance 'foo' → foo.docket.<Domain> (MCP) and relay-foo.docket.<Domain> (relay). Wildcard DNS + Caddy TLS are operator prerequisites (docs/META.md).",
    "Domain": "localhost",
    "//McpImageRepo": "Image repositories (no tag). An instance's ImageTag is appended: {McpImageRepo}:{tag} and {RelayImageRepo}:{tag}.",
    "McpImageRepo": "docket-mcp",
    "RelayImageRepo": "docket-relay",
    "DefaultImageTag": "latest",
    "PostgresImage": "postgres:16",
    "//CaddyAdminUrl": "The edge Caddy admin API base (design note §4). Meta manages route entries within an operator-established base config.",
    "CaddyAdminUrl": "http://localhost:2019",
    "CaddyServerName": "srv0",
    "//MigrateOnStartup": "Apply meta's OWN migration to its OWN store at startup (dev convenience). Unrelated to the per-instance Docket:MigrateOnStartup meta injects into instances.",
    "MigrateOnStartup": false
  }
}
