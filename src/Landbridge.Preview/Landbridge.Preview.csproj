<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- The HTTP preview frontend (spec §8.4) is a standalone, separately-deployable
       module ON TOP of the pure TCP tunnel (§3) — its own ASP.NET host, NOT part
       of Landbridge.Mcp and NOT a change to the pure byte-splicer Landbridge.Relay. It
       reuses the shared Aspire service defaults (health, OTel) like the other
       hosts do. -->
  <ItemGroup>
    <ProjectReference Include="..\Landbridge.ServiceDefaults\Landbridge.ServiceDefaults.csproj" />
    <!-- §8.4: the frontend IS the consumer end. It dials the relay's existing
         /tunnel presenting the same RelayTunnel header names + consumer role
         string, so it shares that vocabulary via Landbridge.Contracts — the same way
         the runner's tunnel client does. It does NOT reference Landbridge.Relay (the
         splicer is untouched) or Landbridge.ControlPlane (it talks to the plane over
         the /preview/connect HTTP contract, like the relay talks to /relay/validate). -->
    <ProjectReference Include="..\Landbridge.Contracts\Landbridge.Contracts.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
