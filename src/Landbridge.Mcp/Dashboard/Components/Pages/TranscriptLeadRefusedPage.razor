<DashboardDocument Title="Transcripts">
<h1>Transcripts are a human surface</h1>
<p class="empty">This session is a Lead credential. Transcripts are served only to a
    human operator session.</p>
<p class="sub">A full transcript is unbounded, unredacted, untrusted text carrying
    everything the agent read — far beyond the bounded worker report a Lead reads with
    <code>get_session_report</code>. Sign in as an operator to read it.</p>
</DashboardDocument>
