<DashboardDocument Title="Team already has a Lead" ActiveKey="connect">
<h1>Team already has a Lead</h1>
<p class="sub">@Reason</p>
<section>
    <p>One live owner per Team. Check takeover only if you mean to evict them —
        their next call is told who did it.</p>
    <p><a href="/dashboard/connect">← back to Connect</a></p>
</section>
</DashboardDocument>

@code {
    [Parameter] public string Reason { get; set; } = "";
}
