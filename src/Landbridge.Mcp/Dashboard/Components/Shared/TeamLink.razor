@if (TeamId == Guid.Empty)
{
    <span class="nt">—</span>
}
else
{
    <a class="mono" href="@TeamHref(TeamId, Slug)" title="@TeamId">@Label(TeamId, Slug)</a>
}

@code {
    [Parameter] public Guid TeamId { get; set; }
    [Parameter] public string? Slug { get; set; }
}
