<Project Sdk="Microsoft.NET.Sdk.Web">

  <!-- The SSE hub (ideas/hub-and-write-queue.md): its own ASP.NET host, NOT
       part of Landbridge.Mcp. LISTENs on session NOTIFY, writes hub_queue, serves
       EventSource. Nothing in the plane calls it yet. -->
  <ItemGroup>
    <ProjectReference Include="..\Landbridge.ServiceDefaults\Landbridge.ServiceDefaults.csproj" />
    <ProjectReference Include="..\Landbridge.ControlPlane\Landbridge.ControlPlane.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

</Project>
